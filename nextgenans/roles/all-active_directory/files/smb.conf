[global]
  workgroup = SYS
  client signing = yes
  client use spnego = yes
  kerberos method = secrets and keytab
  realm = SYS.LCGOSC.COM
  security = ads
  log file = /var/log/samba/%m.log
  max log size = 50