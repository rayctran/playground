#########################################################################################
### This file is managed by Ansible and local changes may be overwritten at any time! ###
#########################################################################################

Job {
  Name = "{{ ansible_hostname }}"
  JobDefs = "{{ bacula_backup_schedule }}-hyb"
  Client = "{{ ansible_hostname }}"
}